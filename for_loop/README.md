# 🌀 Go `for` Loop — Complete Guide

The `for` loop in Go is the **only looping construct** — it can act as a traditional `for`, a `while`, or even an infinite loop.  
It’s one of the most powerful and flexible features of the Go language.

---

## 🧩 Basic Structure

```go
for initialization; condition; post {
    // code to execute each iteration
}
